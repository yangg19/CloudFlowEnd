<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cnpc.server.mapper.AdminMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.cnpc.server.pojo.Admin">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="phone" property="phone" />
        <result column="telephone" property="telephone" />
        <result column="address" property="address" />
        <result column="enabled" property="enabled" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="userFace" property="userFace" />
        <result column="remark" property="remark" />
        <result column="birthday" property="birthday" />
        <result column="workID" property="workID" />
        <result column="email" property="email" />
        <result column="workGroup" property="workGroup" />
        <result column="passQuestion" property="passQuestion" />
        <result column="passAnswer" property="passAnswer" />

    </resultMap>

    <resultMap id="AdminWithRole" type="com.cnpc.server.pojo.Admin" extends="BaseResultMap">
        <collection property="roles" ofType="com.cnpc.server.pojo.Role">
            <id column="rid" property="id" />
            <result column="rname" property="name" />
            <result column="rnameZh" property="nameZh" />
        </collection>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, phone, telephone, address, enabled, username, password, userFace, remark
    </sql>

    <!-- 获取所有操作员-->
    <select id="getAllAdmins" resultMap="AdminWithRole">
        SELECT
        a.*,
        r.id AS rid,
        r.`name` AS rname,
        r.nameZh AS rnameZh
        FROM
        t_admin a
        LEFT JOIN t_admin_role ar ON a.id = ar.adminId
        LEFT JOIN t_role r ON r.id = ar.rid
        <!--<where>-->
        <!--    <if test="null!=keywords and ''!=keywords">-->
        <!--        AND a.`name` LIKE CONCAT( '%', #{keywords}, '%' )-->
        <!--    </if>-->
        <!--</where>-->
        WHERE
        a.id != 1
            <if test="null!=keywords and ''!=keywords">
                AND a.`name` LIKE CONCAT( '%', #{keywords}, '%' )
            </if>
        ORDER BY
        a.id
    </select>

    <!--获取所有员工-->
    <select id="getAllAdmin" resultType="com.cnpc.server.pojo.Admin">
        SELECT
        id, `name`
        FROM
        t_admin
        <!--<where>-->
        <!--    <if test="null!=keywords and ''!=keywords">-->
        <!--        AND a.`name` LIKE CONCAT( '%', #{keywords}, '%' )-->
        <!--    </if>-->
        <!--</where>-->
    </select>

    <!--获取所有员工-->
    <select id="getAdminName" resultType="string">
        SELECT
            `name`
        FROM
            t_admin
        WHERE
            id = #{id}
    </select>

</mapper>